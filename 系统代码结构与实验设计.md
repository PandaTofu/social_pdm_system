# 社交媒体后端预测性维护系统：代码结构与实验设计

## 1. 这次更新解决什么问题

本项目现在把“系统能运行”和“论文结论可复现”分开处理。系统原型负责执行显式Schema解析、流内质量控制、Spark处理、漂移检测、自适应随机森林和告警解释；实验层负责保存指标、预测、阈值曲线、消融结果、性能记录和作图来源。

所有论文图表必须从JSON或CSV实验产物生成。绘图代码不得写入目标F1、目标吞吐量或论文预期数字。若某项实验没有运行，对应图片应缺失，而不是用占位数据补齐。

## 2. 部署边界

当前推荐部署环境是AutoDL单台Linux服务器：

- PySpark 3.5.1，`local[*]`模式；
- Java 17；
- H2O 3.46本地进程；
- NDJSON文件加速回放；
- Flask只读Dashboard；
- 不安装Docker，不运行Kubernetes。

该环境可以验证Schema与质量规则、Spark单节点处理、KS漂移、自适应训练、模型指标、SHAP解释及业务Dashboard。它不能证明Kafka lag、NiFi性能、MongoDB分片吞吐、Kubernetes executor弹性伸缩或多节点横向扩展。

论文若继续保留Kubernetes API自动增加/删除executor pod的描述，必须另建真正的K8s测试环境；否则应把该部分改写为架构设计或未来工作。

## 3. 最新代码结构

```text
social_pdm_system/
├─ apps/
│  ├─ generate_telemetry.py       # 七天、50台逻辑服务器的可复现数据生成器
│  ├─ schema_registry.py          # 版本化Schema查询服务
│  ├─ dashboard.py                # 只读业务监控与SHAP告警API
│  └─ templates/dashboard.html    # 最小Web Dashboard
├─ autodl/
│  ├─ file_streaming_job.py       # 文件源Structured Streaming及微批指标
│  ├─ make_drift_windows.py       # 构建参考/当前特征窗口
│  ├─ replay_to_files.py          # 原子化文件流回放
│  ├─ run_autodl_experiment.sh    # 流处理开发实验
│  └─ run_complete_paper_experiment.sh # 完整论文实验入口
├─ configs/
│  ├─ experiment.yaml             # 数据规模、漂移、突发流量和质量缺陷参数
│  ├─ schema_v1.json
│  └─ schema_v2.json
├─ ml/
│  ├─ adaptive_telemetry_comparison.py # 静态/重训/加权/完整方法消融
│  ├─ drift_monitor.py            # KS D、近似p值和双门槛漂移判定
│  ├─ h2o_binary_metrics.py       # 统一阈值指标、混淆矩阵和曲线输出
│  └─ train_h2o.py
├─ spark/
│  ├─ streaming_job.py            # 显式Schema、五维质量控制、风险评分
│  ├─ cmapss_rf_classification_baseline.py # C-MAPSS科学性验证
│  ├─ cmapss_rul_experiment.py     # 独立RUL回归补充实验
│  └─ system_benchmark.py          # AutoDL单节点吞吐/延迟证据
├─ scripts/
│  └─ generate_complete_paper_figures.py # 完整、可审计的统一绘图入口
├─ tests/
├─ reports/generated/             # 生成图片，不提交硬编码实验结果
└─ data/autodl_runtime/paper_run/ # 完整实验产物，默认不提交Git
```

## 4. 双数据集定位

### 4.1 NASA C-MAPSS

C-MAPSS负责科学性验证。分类任务定义为“当前循环后30个cycle内是否发生故障”，即`RUL <= 30`。训练集根据每台发动机的最终cycle构造RUL；测试集使用NASA提供的官方RUL标签补全每个观测cycle的实际RUL。

输出包括：

- 训练/测试行数与发动机数；
- 正类数量、正类比例和RUL分段；
- Precision、Recall、F1、Accuracy、ROC-AUC、PR-AUC；
- TN、FP、FN、TP；
- 阈值曲线；
- 每条测试记录的预测概率。

C-MAPSS结果不能与模拟社交数据的F1直接比较，因为数据、标签和正类比例不同。

### 4.2 模拟社交服务器数据

模拟数据负责领域实用性验证。默认配置为7天、50台逻辑服务器、1分钟采样，包含Schema v1→v2变化、第五天概念漂移、5倍/10倍突发流量、迟到、重复、缺失和范围错误。

时间切分固定为：

- 第1-3天：静态模型训练；
- 第5天：漂移后的延迟反馈，按事件ID固定切分70%重训、30%阈值校准；
- 第6-7天：完全留出的最终测试集。

任何训练、加权和阈值选择都不能使用第6-7天标签。

## 5. 模型比较与消融

完整实验比较四个变体：

1. `static_rf`：仅第1-3天训练，阈值0.5；
2. `unweighted_retraining`：加入第5天反馈训练，不加近期权重，阈值0.5；
3. `weighted_retraining`：第5天样本权重为历史样本4倍，阈值0.5；
4. `full_adaptive`：加权重训，并仅使用第5天校准子集选择F1阈值。

所有变体在同一个第6-7天测试集上报告Precision、Recall、F1、PR-AUC和混淆矩阵。该设计能够区分“重训本身”“近期权重”和“阈值校准”分别贡献了多少提升。

## 6. 漂移判定

`drift_monitor.py`同时报告：

- KS D统计量，作为实际分布变化的效应量；
- 两样本KS渐近p值，作为统计显著性证据；
- 参考窗口与当前窗口样本数。

默认漂移条件为`D > 0.20`且`p < 0.05`。大样本下极小差异也可能获得很小p值，所以不能只根据p值触发重训练。

## 7. Spark性能证据

性能报告分为两类：

- Structured Streaming微批指标：每个batch的received、accepted、quarantined、实际处理时长和rows/s；
- 离线可重复规模实验：5万、10万、25万、50万行下的单节点批处理延迟和吞吐量。

`system_benchmark.json`必须保留Spark版本、master、并行度和范围声明。论文中只能报告实际运行值，并注明AutoDL硬件配置。不能把单节点结果描述成K8s扩缩容结果。

## 8. 图表及来源

| 文件 | 证据来源 | 用途 |
|---|---|---|
| `fig1_enhanced_architecture` | 代码结构 | 三个集成边界及处理链路 |
| `fig2_cmapss_distribution` | C-MAPSS metrics JSON | 标签/RUL分布 |
| `fig3_cmapss_validation` | C-MAPSS metrics + predictions | 混淆矩阵和PR曲线 |
| `fig4_ks_drift_detection` | drift report | 特征漂移 |
| `fig5_model_comparison` | adaptive report | 四变体综合指标 |
| `fig6_threshold_calibration` | H2O阈值曲线 | 阈值选择依据 |
| `fig7_model_stability` | 第6-7天逐日指标 | 观测到的漂移后稳定性 |
| `fig8_system_performance` | system benchmark | 单节点吞吐与延迟 |
| `fig9_shap_explanations` | H2O贡献CSV | 全局和局部解释 |

每张图同时输出300 DPI PNG和矢量PDF。`figure_manifest.json`记录全部输入路径；`table_reproducible_results.md`保存与图一致的数值表。

## 9. Web Dashboard

论文原文多次要求预测和SHAP解释呈现在monitoring dashboard，因此保留原文时应实现最小Web界面。当前Dashboard提供：

- 最新Spark微批处理速率；
- KS漂移状态；
- 静态与自适应模型指标；
- 实际部署阈值；
- 预测告警及前三个TreeSHAP贡献特征。

Dashboard只读取持久化实验文件。数据缺失时明确显示不可用，不生成模拟在线指标。Spark 4040/6006 UI用于工程诊断，不能替代该业务界面。

## 10. AutoDL完整运行

```bash
cd /root/autodl-tmp/social_pdm_system
export PDM_ENV=/root/autodl-tmp/envs/social-pdm
export JAVA_HOME=/usr/lib/jvm/java-17-openjdk-amd64
export PATH="$PDM_ENV/bin:$JAVA_HOME/bin:$PATH"

git pull --ff-only
python -m pip install -r autodl/requirements-autodl.txt
bash autodl/run_complete_paper_experiment.sh \
  data/development.ndjson data/CMAPSSData \
  data/autodl_runtime/paper_run reports/generated
```

如果服务器没有`data/development.ndjson`，脚本会按`configs/experiment.yaml`自动生成。如果没有C-MAPSS文件，脚本会停止并报告缺失路径，不会替换为其他数据。

## 11. 启动Dashboard

```bash
cd /root/autodl-tmp/social_pdm_system
export PDM_RUNTIME=data/autodl_runtime/paper_run
python -m flask --app apps.dashboard run --host 0.0.0.0 --port 8090
```

本地浏览器通过AutoDL自定义服务或SSH端口转发访问8090端口。Dashboard不是实验前置依赖；实验完成后再启动即可。

## 12. 论文结果使用规则

1. 第5章的数值必须来自本次`paper_run`目录；
2. 论文目标值与实测值必须分列，不能把目标值写成已实现结果；
3. 图表、表格和正文使用同一个JSON/CSV来源；
4. 不同数据集的F1不能直接作高低比较；
5. SHAP生成失败时必须报告H2O运行时不支持，不能用随机特征重要性替代；
6. Git仓库提交代码、配置和设计文档，不提交411 MB模拟数据、C-MAPSS原始文件或大体积运行目录。
