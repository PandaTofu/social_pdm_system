# 模拟数据诊断与改进建议

## 1. 本次审计的数据来源

本报告基于AutoDL实际产物，而不是论文预设目标值：

- 评估协议：第1–3天训练，第5天反馈数据按70/30拆分为重训练集和阈值校准集，第6–7天最终评估；
- 评估集：141,167条记录、50台逻辑服务器；
- 正例：529条，正例率0.3747%；
- 第6–7天没有正例记录的逻辑服务器：32台；
- 故障标签：未来30分钟内是否发生故障。

完整可机读诊断由`scripts/analyze_simulated_results.py`生成，包括JSON和Markdown两种格式。

## 2. 为什么Recall很高而Precision、F1较低

| 方案 | Precision | Recall | F1 | PR-AUC | FPR | 告警率 | FP/TP |
|---|---:|---:|---:|---:|---:|---:|---:|
| static_rf，阈值0.5 | 0.1734 | 1.0000 | 0.2955 | 0.2761 | 1.7933% | 2.1613% | 4.77 |
| weighted_retraining，阈值0.5 | 0.1734 | 1.0000 | 0.2956 | 0.3078 | 1.7925% | 2.1606% | 4.77 |
| full_adaptive，阈值0.7739 | 0.2544 | 0.9055 | 0.3972 | 0.3078 | 0.9983% | 1.3339% | 2.93 |
| unweighted_retraining，阈值0.5 | 0.1733 | 1.0000 | 0.2954 | 0.3004 | 1.7940% | 2.1620% | 4.77 |

这组结果在数学上没有矛盾。评估集的正例率只有0.3747%，即使FPR仅约1.79%，140,638个负例仍会产生约2,522个假阳性；529个正例全部被检出时，Precision也只有529/(529+2522)=0.1734。自适应阈值将FP降到1,404、TP保留479，因此Precision和F1提高，但仍有约2.93个假告警对应1个真告警。

PR-AUC必须与随机基线0.003747一起解释。当前PR-AUC 0.2761–0.3078约为随机基线的74–82倍，说明排序能力并不差；问题主要集中在类别先验、固定阈值和告警负担，而不是模型完全无效。

## 3. 当前模拟设计的结构性问题

### 3.1 特征预警窗口和标签窗口不一致

生成器在故障前90分钟统一增强CPU、队列、磁盘和错误率等特征，但只有故障前30分钟被标记为正例。因此故障前31–90分钟的记录具有明显故障特征，标签却为0，会形成结构性假阳性。这是Precision受限的重要原因。

建议将故障征兆改为随剩余时间逐渐增强：90–31分钟只有弱征兆，30分钟内出现强征兆；或者让强征兆窗口与标签窗口严格一致。推荐前者，因为更符合渐进退化过程。

### 3.2 负例下采样改变了训练先验

为控制H2O内存，训练阶段保留全部正例、只保留8%负例。训练正例率因此从真实评估先验0.3747%提高到约2.8%–3.4%。模型输出概率会偏向训练先验，0.5不再是自然的部署阈值。

建议保留该采样策略以控制资源，但必须：

1. 在独立校准集上做阈值选择或概率校准；
2. 同时报告请求阈值、实际阈值、告警率和FPR；
3. 在论文中明确下采样比例及训练/评估先验；
4. 增加按业务成本选择阈值的实验，而不是只最大化F1。

### 3.3 当前更接近协变量漂移而非概念漂移

部署后所有记录的`response_p95_ms`和`error_rate`整体升高，但故障前的核心生成机制没有真正切换，因此主要是`P(X)`变化。重训练模型在相同0.5阈值下F1几乎不变，也支持这一判断。当前结果可证明KS漂移检测和阈值自适应有效，但不足以单独证明重训练解决了概念漂移。

若论文主张概念漂移，应显式改变部署前后的`P(y|X)`：例如部署前以CPU/队列为主要故障征兆，部署后改为错误率/GC暂停/缓存命中率，同时降低旧特征的预测作用。漂移强度应参数化，并设置无漂移、轻度和重度三档。

### 3.4 测试期正例覆盖不足

第6–7天有32/50台逻辑服务器没有正例记录，单次随机种子下的结果容易受少量事故时刻影响。正式论文建议运行至少5个随机种子，报告均值、标准差或95%置信区间；并让事故计划在训练、漂移反馈和测试阶段均有最低覆盖量。

## 4. 是否符合预期

当前数据适合证明以下内容：

- Spark显式Schema、质量规则、去重和流式处理；
- 流量突发与Schema演进处理；
- 稀有事件条件下的告警评估；
- KS协变量漂移检测；
- 阈值校准可以降低假阳性和告警负担。

在修改前，不宜声称它已经充分证明：

- 重训练本身显著优于静态模型；
- 系统有效解决了概念漂移；
- 单次7天、50台逻辑服务器的结果具有稳定的统计外推性。

## 5. 下一轮推荐实验

1. 修订渐进式故障征兆和可配置概念漂移机制；
2. 保留现有数据作为`scenario_covariate_shift_v1`，新增`scenario_concept_drift_v2`，避免覆盖历史结果；
3. 每个场景运行5个随机种子；
4. 同时报告Precision、Recall、F1、PR-AUC、FPR、告警率、FP/TP和漂移检测延迟；
5. 将静态RF、仅重训练、加权重训练、重训练+阈值校准作为消融链；
6. C-MAPSS继续承担科学有效性验证，模拟数据承担领域实用性和系统工程验证。

## 6. 已实现的场景版本

### `covariate_shift_v1`

配置为`configs/scenarios/covariate_shift_v1.json`。该文件冻结原始随机种子和全部参数，保留90分钟统一故障征兆、30分钟标签以及部署后的整体指标漂移，用于复现历史结果，不再覆盖。

### `concept_drift_v2`

配置为`configs/scenarios/concept_drift_v2.json`，主要变化如下：

- 故障征兆和标签窗口均为30分钟，征兆强度随故障临近从0渐进到1；
- 每台逻辑服务器在第1–3天、第5天、第6–7天各安排一次事故，保证训练、反馈和测试阶段均有正例覆盖；
- Spark实验会话固定为UTC，使模型切分日与生成器的UTC逻辑日严格一致，不受云服务器本地时区影响；
- 部署前主要风险信号为CPU、队列和磁盘；部署后这些信号减弱，响应延迟、错误率和超时成为主要风险信号；
- 旧数据和旧输出目录不会被覆盖，场景运行器检测到目标已存在时会终止。

三台逻辑服务器的小规模生成器验证显示：部署前正例的平均CPU/队列明显上升；部署后正例的平均`response_p95_ms`约为负例的2.1倍，`error_rate`约为负例的6.8倍，而CPU差异显著减弱，符合预设概念漂移方向。正式结论仍以AutoDL完整Spark/H2O实验为准。
