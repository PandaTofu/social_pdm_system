# Social PDM 云服务器部署指南（Git + 轻量 Docker）

本文面向 Ubuntu 22.04 云服务器。当前服务器为 **2 vCPU、1.6 GiB RAM、约 19 GiB 可用磁盘**，因此建议先完成代码下载、容器构建和小规模冒烟测试，不要直接启动完整 `docker-compose.yml`，也不要运行 100 台逻辑服务器、30 天的正式实验。

Docker 容器在这里相当于可复现的隔离运行环境，但它不是 Python `venv`。Java 17、PySpark 3.5.1、H2O 和绘图库都安装在镜像中，不修改宿主机 Python 或 Java。

## 1. 登录服务器

在本地 PowerShell 中执行：

```powershell
ssh root@121.43.54.63
```

首次连接输入 `yes`，随后输入服务器密码。不要把密码写在脚本、Git 配置或 `.env` 中。建议部署完成后修改 root 密码，并改用 SSH 密钥。

## 2. 检查 Docker 和服务器资源

```bash
apt-get update
apt-get install -y docker-compose-plugin

docker version
docker compose version
free -h
df -h /
nproc
```

如果 Docker 服务未启动：

```bash
systemctl enable --now docker
docker info
```

若 `docker compose version` 不存在，需要安装 Compose plugin；优先使用云厂商软件源或服务器已经配置好的 Docker 软件源，不要混装多个 Docker 发行版。

## 3. 为小内存服务器配置 Swap（推荐）

1.6 GiB 内存容易在 Docker 构建或启动 Spark JVM 时触发 OOM。下面创建 4 GiB Swap：

```bash
fallocate -l 4G /swapfile
chmod 600 /swapfile
mkswap /swapfile
swapon /swapfile
grep -q '^/swapfile ' /etc/fstab || echo '/swapfile none swap sw 0 0' >> /etc/fstab
free -h
```

Swap 只能降低进程被杀死的概率，不能让这台服务器具备正式实验所需性能。

## 4. 通过 GitHub 下载代码

安装 Git：

```bash
apt-get update
apt-get install -y git ca-certificates curl
git --version
```

将代码放到 `/opt/social_pdm_system`：

```bash
mkdir -p /opt
cd /opt
git clone --depth 1 --branch main https://github.com/PandaTofu/social_pdm_system.git
cd /opt/social_pdm_system
git remote -v
git rev-parse --short HEAD
```

如果目录已经存在，不要直接删除。先检查：

```bash
cd /opt/social_pdm_system
git status --short
git pull --ff-only
```

如果 `git pull` 提示未跟踪文件会被覆盖，先把这些文件移动到备份目录，再执行拉取：

```bash
mkdir -p /root/social_pdm_backup
mv <冲突文件> /root/social_pdm_backup/
git pull --ff-only
```

不要使用 `git reset --hard`，否则可能丢失服务器上的实验结果。

### GitHub 连接超时怎么办

先验证网络和 DNS：

```bash
getent hosts github.com
curl -I --connect-timeout 15 https://github.com
git ls-remote https://github.com/PandaTofu/social_pdm_system.git HEAD
```

如果 HTTPS 长期不可用，可以从本地 PC 通过 SCP 上传仓库压缩包，或配置 GitHub SSH key 后使用：

```bash
git clone --depth 1 git@github.com:PandaTofu/social_pdm_system.git
```

不建议使用来源不明的 GitHub 镜像站，因为代码和依赖存在供应链风险。

## 5. 构建轻量实验容器

本项目新增了 `Dockerfile.experiment` 和 `compose.experiment.yml`。它们只创建实验运行环境，不启动 Kafka、NiFi、MongoDB、MinIO、Grafana 或独立 Spark 集群。

```bash
cd /opt/social_pdm_system
docker compose -f compose.experiment.yml build
docker compose -f compose.experiment.yml up -d
docker compose -f compose.experiment.yml ps
```

首次构建需要下载 Python、Java 和 Python 依赖。检查镜像与容器：

```bash
docker images social-pdm-experiment
docker compose -f compose.experiment.yml logs --tail=100 experiment
```

进入容器：

```bash
docker compose -f compose.experiment.yml exec experiment bash
```

在容器内检查环境：

```bash
python --version
java -version
spark-submit --version
python -c "import pyspark, h2o, matplotlib; print(pyspark.__version__, h2o.__version__)"
exit
```

容器限制为 1.5 CPU、1200 MiB 内存，Dashboard 仅绑定宿主机 `127.0.0.1:8090`，不会直接暴露到公网。

## 6. 只做部署冒烟测试

先生成 2 台逻辑服务器、2 天、10 分钟采样的小数据。它只有约 576 条基础记录，同时覆盖 Schema v1 和 v2，但不是论文正式实验数据：

```bash
cd /opt/social_pdm_system
mkdir -p data/smoke
docker compose -f compose.experiment.yml exec -T experiment \
  python apps/generate_telemetry.py \
  --config configs/scenarios/smoke_2servers_2days.json \
  --output-dir data/smoke/telemetry
```

验证数据契约：

```bash
docker compose -f compose.experiment.yml exec -T experiment \
  python tests/validate_contract.py data/smoke/telemetry
```

只验证 Spark 能否启动，不运行论文模型：

```bash
docker compose -f compose.experiment.yml exec -T experiment \
  spark-submit --master 'local[1]' --driver-memory 512m --version
```

查看生成文件：

```bash
find data/smoke -maxdepth 3 -type f -ls
du -sh data/smoke
```

## 7. 上传 NASA C-MAPSS 数据

C-MAPSS 数据通常不放入 Git 仓库。在本地 PowerShell 中执行：

```powershell
scp -r "D:\Work\2608月订单\Spark论文修改\social_pdm_system\data\CMAPSSData" root@121.43.54.63:/opt/social_pdm_system/data/
```

然后在服务器检查 FD001–FD004 文件是否齐全：

```bash
cd /opt/social_pdm_system
for subset in FD001 FD002 FD003 FD004; do
  test -f "data/CMAPSSData/train_${subset}.txt" && echo "$subset train OK"
  test -f "data/CMAPSSData/test_${subset}.txt" && echo "$subset test OK"
  test -f "data/CMAPSSData/RUL_${subset}.txt" && echo "$subset RUL OK"
done
```

本节只上传和检查数据，不建议在当前 1.6 GiB 服务器上运行四个子集的 H2O 对比。

## 7.1 Docker Hub 镜像不可用时直接部署

如果阿里云 ACR 加速器对 `python:3.11-slim-bookworm` 返回 `not found`，可直接使用 Ubuntu 软件源和项目内虚拟环境。该方式不修改系统默认 Python 版本：

```bash
cd /opt/social_pdm_system
apt-get update
apt-get install -y --no-install-recommends \
  openjdk-17-jre-headless python3-venv python3-pip

python3 -m venv .venv
.venv/bin/python -m pip install \
  -i https://pypi.tuna.tsinghua.edu.cn/simple \
  --upgrade pip setuptools wheel
.venv/bin/python -m pip install \
  -i https://pypi.tuna.tsinghua.edu.cn/simple \
  -r autodl/requirements-autodl.txt
```

每次重新登录后激活环境：

```bash
cd /opt/social_pdm_system
source .venv/bin/activate
export JAVA_HOME=/usr/lib/jvm/java-17-openjdk-amd64
export PATH="$JAVA_HOME/bin:$PATH"
export SPARK_LOCAL_IP=127.0.0.1
```

检查版本：

```bash
python --version
java -version
spark-submit --version
```

Ubuntu 22.04 默认 Python 为 3.10，依赖文件会自动选择兼容的 Matplotlib 3.10.9；Python 3.11 环境继续使用 Matplotlib 3.11.1。这不会改变 Spark/H2O 模型指标。

## 8. Dashboard 的安全访问方式

实验结果存在后，可在容器中启动 Dashboard：

```bash
docker compose -f compose.experiment.yml exec -T experiment \
  env PDM_RUNTIME=data/autodl_runtime/paper_run \
  python -m flask --app apps.dashboard run --host 0.0.0.0 --port 8090
```

因为端口只绑定到服务器 `127.0.0.1`，在本地另开 PowerShell 建立 SSH 隧道：

```powershell
ssh -L 8090:127.0.0.1:8090 root@121.43.54.63
```

浏览器访问 `http://127.0.0.1:8090`。不要把 Flask 开发服务器或 Spark UI 直接开放到公网。

## 9. 停止、重启和更新

```bash
cd /opt/social_pdm_system

# 停止并保留代码和数据
docker compose -f compose.experiment.yml down

# 再次启动
docker compose -f compose.experiment.yml up -d

# 更新代码；有本地改动时先处理 git status
git status --short
git pull --ff-only
docker compose -f compose.experiment.yml build
docker compose -f compose.experiment.yml up -d
```

不要执行 `docker compose down -v` 或大范围 `docker system prune`，除非已经确认所有实验数据和镜像均可删除。

## 10. 什么时候使用完整 Docker Compose

仓库原有 `docker-compose.yml` 会启动 Kafka、MongoDB、MinIO、NiFi、H2O、Spark Master/Worker、Prometheus 和 Grafana。当前 Spark Worker 配置了 6 GiB 内存，整套服务建议至少使用：

- 8 vCPU；
- 16 GiB RAM，推荐 32 GiB；
- 80–100 GiB 可用磁盘。

升级服务器后才执行：

```bash
cp .env.example .env
chmod 600 .env
nano .env
docker compose pull
docker compose build schema-registry generator
docker compose up -d
docker compose ps
```

当前单机部署不需要 Kubernetes。K8s 只用于以后验证多节点调度或自动扩缩容，不是生成论文核心算法结果的前置条件。

## 11. 当前服务器的验收边界

在这台服务器上，以下结果表示部署成功：

1. Git 仓库下载完成；
2. `social-pdm-experiment:3.5.1` 镜像构建成功；
3. Python、Java、PySpark、H2O 均能打印版本；
4. 冒烟数据能够生成并通过数据契约检查；
5. Spark `local[1]` 能启动并正常退出。

这不等于论文正式实验已经完成，也不能用来证明吞吐量、端到端延迟、Kubernetes 扩缩容或 FD001–FD004 模型性能。
