# 社交媒体后端预测性维护系统：Linux 云服务器部署指南

本指南面向一台 Ubuntu 22.04/24.04 云服务器，部署 Docker Compose 原型。它会启动 Kafka、MongoDB、MinIO、NiFi、H2O、Spark、Prometheus、Grafana、模拟数据生成器与 Schema Registry。**它不适用于 AutoDL 普通容器；AutoDL 用户请使用 `AutoDL部署指南.md`。**

> 建议最低规格为 8 vCPU、32 GB RAM、100 GB SSD。若机器资源较小，可先停止 NiFi、H2O、Prometheus/Grafana，仅验证 Kafka、MongoDB 和 Spark 数据流。

## 1. 登录和基础准备

在本机终端连接云服务器：

```bash
ssh <用户名>@<云服务器公网IP>
sudo apt update && sudo apt upgrade -y
sudo apt install -y ca-certificates curl git openssl
```

如使用 UFW，先允许 SSH；不要直接把管理界面向全网开放：

```bash
sudo ufw allow OpenSSH
sudo ufw enable
# 将 <你的公网IP> 替换为自己的固定公网 IP，再按需开放管理界面
sudo ufw allow from <你的公网IP> to any port 3000 proto tcp  # Grafana
sudo ufw allow from <你的公网IP> to any port 8080 proto tcp  # Spark UI
sudo ufw allow from <你的公网IP> to any port 8443 proto tcp  # NiFi
sudo ufw status
```

不要对公网开放 Kafka `9092`、MongoDB `27017` 或 MinIO API `9000`。生产环境应通过 VPN、堡垒机或反向代理访问 UI。

## 2. 安装 Docker Engine 和 Compose

以下命令适用于 Ubuntu。若云厂商镜像已安装 Docker，先执行 `docker --version` 确认即可。

```bash
sudo install -m 0755 -d /etc/apt/keyrings
curl -fsSL https://download.docker.com/linux/ubuntu/gpg | \
  sudo gpg --dearmor -o /etc/apt/keyrings/docker.gpg
sudo chmod a+r /etc/apt/keyrings/docker.gpg

echo "deb [arch=$(dpkg --print-architecture) signed-by=/etc/apt/keyrings/docker.gpg] \
https://download.docker.com/linux/ubuntu $(. /etc/os-release && echo \"$VERSION_CODENAME\") stable" | \
  sudo tee /etc/apt/sources.list.d/docker.list > /dev/null

sudo apt update
sudo apt install -y docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
sudo usermod -aG docker "$USER"
newgrp docker
docker version
docker compose version
```

## 3. 下载代码与配置密钥

```bash
git clone https://github.com/PandaTofu/social_pdm_system.git
cd social_pdm_system
cp .env.example .env
chmod 600 .env
nano .env
```

在 `.env` 中替换所有默认密码。可用下面的命令生成安全密码，再复制到 `.env`：

```bash
openssl rand -hex 24
```

`.env` 只保留在服务器中，绝不能 `git add` 或上传。

## 4. 启动服务

首次拉取镜像需要较长时间和稳定网络。运行：

```bash
docker compose pull
docker compose build schema-registry generator
docker compose up -d
docker compose ps
```

检查服务日志；若某服务未启动，先看其日志，而不是反复重启全部服务：

```bash
docker compose logs --tail=100 kafka
docker compose logs --tail=100 spark-master
docker compose logs --tail=100 mongo
docker compose logs --tail=100 nifi
```

可在浏览器访问以下界面。建议只通过防火墙白名单、VPN 或 SSH 隧道访问。

| 服务 | 地址 |
|---|---|
| Spark Master UI | `http://<公网IP>:8080` |
| Spark Worker UI | `http://<公网IP>:8081` |
| NiFi | `https://<公网IP>:8443/nifi` |
| H2O Flow | `http://<公网IP>:54321` |
| MinIO Console | `http://<公网IP>:9001` |
| Grafana | `http://<公网IP>:3000` |

## 5. 创建 Kafka topic 并提交 Spark 流任务

```bash
bash scripts/create_topics.sh
mkdir -p logs
bash scripts/submit_spark.sh > logs/spark-streaming.log 2>&1 &
tail -f logs/spark-streaming.log
```

Spark 首次提交会下载 Kafka/MongoDB connector；完成后应在 Spark UI 看到 `social-pdm-streaming` 的 driver 和 worker。若看到 connector 版本错误，确认 Spark image 是 3.5.1，并且脚本中的 Scala 2.12 connector 没有被修改。

## 6. 生成并发送开发数据

在另一个 SSH 窗口执行：

```bash
cd ~/social_pdm_system
docker compose exec -T generator python /app/apps/generate_telemetry.py \
  --config /app/configs/experiment.yaml \
  --kafka-bootstrap kafka:29092
```

该配置会生成 50 个逻辑服务器、7 天的加速回放数据；它们不是 50 台真实云服务器。完成后检查 Kafka、Spark 与 MongoDB：

```bash
docker compose exec -T kafka kafka-consumer-groups \
  --bootstrap-server kafka:29092 --all-groups --describe

docker compose exec -T mongo mongosh \
  -u "$(grep '^MONGO_ROOT_USERNAME=' .env | cut -d= -f2)" \
  -p "$(grep '^MONGO_ROOT_PASSWORD=' .env | cut -d= -f2)" \
  --authenticationDatabase admin --eval 'db.getSiblingDB("pdm").predictions.countDocuments()'

docker compose logs --tail=150 spark-master
```

有效事件会按 `event_date`、`data_center_id` 写入 Spark 共享 volume 下的 Parquet；无效事件会进入 `telemetry-quarantine`；预测会进入 `predictions` topic 和 MongoDB 的 `pdm.predictions` collection。

## 7. 首次验收检查

1. Schema Registry：`curl http://localhost:8088/schemas` 返回版本 1、2。
2. Kafka：三个 topic 均已存在，consumer lag 随 Spark 消费下降。
3. Spark UI：无持续失败 task；关注 Executors 页的 GC time、Stages 页的 spill 与 shuffle。
4. MongoDB：`pdm.predictions` 中有预测记录。
5. Quarantine：消费 `telemetry-quarantine`，确认能看到缺失字段或 CPU 越界的质量失败原因。
6. 将每次实验的 `logs/spark-streaming.log`、Spark event log、Kafka lag 和配置文件存档，供论文追溯。

## 8. 停止、更新和清理

```bash
# 停止容器，但保留 Kafka/MongoDB/MinIO 数据卷
docker compose down

# 更新代码后重新构建自定义 Python 镜像
git pull --ff-only
docker compose build schema-registry generator
docker compose up -d

# 危险：同时删除所有本项目的数据卷；仅在确认不需要实验数据后使用
# docker compose down -v
```

## 9. 是否使用 Kubernetes？

**当前默认部署不使用 Kubernetes。** 本仓库的 Docker Compose 部署是单台 Linux 原型的首选方案，足以完成模拟数据、Kafka→Spark 数据流、质量控制、漂移检测和单节点实验。

仓库中的 `k8s/streaming-sparkapplication.yaml` 是第二阶段迁移模板。只有在以下情况才需要 Kubernetes：

1. 论文保留“通过 Kubernetes API 自动增加/减少 Spark executor Pod”的实现声明；或
2. 需要在至少两个 worker 节点上验证真正的横向扩缩容。

单台服务器可部署单节点 k3s 来验证 Spark on Kubernetes 的打包与调度，但它不能证明横向扩容。因此，本论文现阶段应把 Docker Compose 结果称为 **single-node cloud prototype evaluation**；Kubernetes 扩缩容结论应等多节点实验完成后再写入。
