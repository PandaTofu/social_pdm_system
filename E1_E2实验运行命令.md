# E1/E2 实验运行命令

本文档对应论文的两个边界增强：

- E1：版本化显式 Schema 与五类质量检查；
- E2：无损 Stream/Batch 任务路由与固定单节点容量下的负载调节。

E2 输出的是单节点调度与资源重新分配证据，不等同于多节点 Kubernetes 水平扩容。

## 1. 更新代码

```bash
cd /root/social_pdm_system
git pull --ff-only origin main
```

如果项目位于 `/root/autodl-tmp/social_pdm_system`，请将上述路径替换为实际路径。

## 2. 激活环境

```bash
cd /root/social_pdm_system
source .venv/bin/activate
export JAVA_HOME=/usr/lib/jvm/java-17-openjdk-amd64
export PATH="$JAVA_HOME/bin:$PATH"
java -version
python --version
python -c "import pyspark; print(pyspark.__version__)"
```

## 3. 先运行 smoke 实验

```bash
bash autodl/run_e1_e2_smoke.sh
```

主要检查文件：

```bash
cat data/autodl_runtime/e1_e2_smoke/results/quality/quality_report.json
cat data/autodl_runtime/e1_e2_smoke/results/routing/routing_report.json
cat data/autodl_runtime/e1_e2_smoke/results/workload/workload_report.json
```

smoke 验收条件：

- 五个质量类别均有 `truth_count`；
- quality Precision、Recall 和 clean-record preservation 均可计算；
- `lossless=true`；
- `routing_accuracy` 可计算；
- controller trace 同时记录 normal allocation 和 stream prioritisation。

## 4. 单独运行正式 E1/E2 实验

如果 30 天原始模拟数据尚未生成：

```bash
python apps/generate_telemetry.py \
  --config configs/scenarios/concept_drift_30d_v4.json \
  --output-dir data/autodl_runtime/scenarios/concept_drift_30d_v4/telemetry
```

执行 Spark 作业：

```bash
spark-submit \
  --master 'local[12]' \
  --driver-memory 10g \
  --conf spark.sql.shuffle.partitions=48 \
  spark/e1_e2_experiment.py \
  --source data/autodl_runtime/scenarios/concept_drift_30d_v4/telemetry \
  --output-dir data/autodl_runtime/scenarios/concept_drift_30d_v4/results/e1_e2
```

该作业还会生成：

- `quality/accepted/`：供 KS 和 E3 模型实验继续使用的数据；
- `quality/quarantine/`：带真值、规则原因和检测类别的隔离记录；
- `routing/audit/`：每条 accepted 记录的路由、规则、原因和决策时间；
- `workload/controller_trace.csv`：固定物理容量下的资源分配决策轨迹。

## 5. 运行完整 30 天链路

完整脚本已经按以下顺序串联：生成数据 → E1/E2 → accepted 数据上的 KS/E3 → Spark 性能实验 → 图表。

```bash
bash autodl/run_30d_v4_60gb.sh
```

正式运行会拒绝覆盖已有目录。需要重跑时请换用新的结果路径，保留旧产物用于审计，不要直接覆盖。

## 6. Spark UI 检查

运行期间重点查看：

- SQL 页面是否使用 Adaptive Query Execution；
- Stages 页面是否发生磁盘 spill；
- 各 task 耗时是否存在明显长尾；
- Executors 页面 GC time 是否低于总 task time 的约 10%；
- 输入和 shuffle 分区是否约为 48。

如果出现磁盘 spill 或明显数据倾斜，应先保存 Spark event log，再调整分区数或内存后重新运行。
