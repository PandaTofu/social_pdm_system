# 社交媒体后端预测性维护系统：详细系统设计

## 1. 范围与证据边界

本系统实现论文提出的 NiFi、MongoDB、Apache Spark、Kubernetes 和 H2O.ai 架构的**单机 Docker Compose 原型**。Kafka 与 MinIO 是为实现可回放流处理、checkpoint 和 Parquet 归档而新增的基础设施组件。CMAPSS 与模拟后端遥测严格分开：前者验证方法的科学严谨性，后者验证三项架构增强的领域实用性。

单机原型可测量真实 Kafka→Spark 的延迟、吞吐、质量隔离和漂移检测；它不能证明多节点横向扩缩容。Kubernetes 目录提供迁移接口，只有在多节点或托管 Kubernetes 上运行时，executor 扩缩容结果才可用于横向可扩展性结论。

## 2. 运行时数据流

```text
Generator / real telemetry
  -> NiFi (optional ingress) -> Kafka telemetry-raw
  -> PySpark streaming job
       -> explicit v1/v2 schema + quality gates
       -> valid Parquet (MinIO/S3) + MongoDB predictions
       -> Kafka telemetry-quarantine
       -> Kafka predictions
  -> drift monitor -> H2O retraining trigger
  -> Prometheus/Grafana + Spark UI/History Server
```

`event_time` 是业务事件发生时间，`ingest_time` 是生成/接入时间。作业以 `event_time` 建 watermark，并以 `event_id` 去重。所有无效事件均附带 `quality_failure_type` 写入 quarantine topic；禁止静默丢弃。

## 3. 数据契约

- `telemetry-raw`：JSON 事件，必须有 `event_id`、`event_time`、`schema_version`、`server_id`、资源与应用指标。
- Schema v2 在 v1 基础上新增 `cache_hit_ratio`、`gc_pause_ms`；其余字段保持向后兼容。
- 预测目标为 `failure_within_30min`，由事件时刻之后 30 分钟内的严重 incident 推导；实时推理输入不携带此字段。
- 字段完整定义见 `configs/schema_v1.json` 与 `configs/schema_v2.json`。

## 4. 三项增强与实现映射

| 论文增强 | 本系统组件 | 可审计输出 |
|---|---|---|
| E1：Schema Registry + 五维质量控制 | `schema_registry`、`spark/streaming_job.py` | schema 版本、接纳/隔离数、失败原因、质量分数 |
| E2：动态资源 + 规则路由 | `traffic_campaign_flag`、Kafka 分区、Spark Dynamic Allocation/K8s 配置 | Kafka lag、batch duration、吞吐、P95/P99、executor 数 |
| E3：KS 漂移 + 可解释/重训 | `ml/drift_monitor.py`、`ml/train_h2o.py`、H2O Flow | KS D、触发时间、模型版本、重训日志、F1/PR-AUC |

## 5. 部署模式

### 本地/单台 Linux 原型

使用 `docker compose up -d` 启动 Kafka、MongoDB、MinIO、NiFi、H2O、Spark master/worker、Prometheus 与 Grafana。该模式用于开发、可重复实验和单节点性能结果。

### AutoDL 普通容器（无 Docker）

使用 `autodl/file_streaming_job.py` 的 Spark File Source：生成器将 NDJSON 原子写入 inbox，PySpark `local[*]` 完成相同的显式 schema、质量隔离和风险评分，并将结果写入本地 Parquet/JSON。该 profile 不启动 Kafka、NiFi、MongoDB、MinIO、Grafana 或 Kubernetes；因此只报告文件摄入率、micro-batch duration、CPU/memory 和质量/预测指标。命令见 `AutoDL部署指南.md`。

### Kubernetes 迁移

将 `spark/streaming_job.py` 构建进 Spark 镜像，通过 `spark-submit --master k8s://...` 提交。启用 `spark.dynamicAllocation.enabled=true` 和 shuffle tracking。Kafka、MongoDB、MinIO 可使用托管服务或 StatefulSet。只在至少两个 worker 节点时报告横向扩缩容结果。

## 6. 分区、存储与调优

- Kafka `telemetry-raw` 初始 6 partitions；单机可调为 3，生产按总 executor core 数和负载调优。
- Spark 不使用 schema inference 或 Python UDF；全部采用 `StructType` 和内置 SQL functions。
- 有效事件按 `event_date`、`data_center_id` 写 Snappy Parquet；历史查询可触发 partition pruning。
- `checkpointLocation` 与 Spark event log 放在 MinIO/S3；MongoDB 保存预测/incident 查询，不替代湖仓归档。
- 开发起点为 2 executors × 2 cores × 6 GB，`spark.sql.shuffle.partitions=48`。在 Spark UI 检查 spill、GC、shuffle 和 task skew 后再变更参数。

## 7. 实验执行与可重复性

实验配置位于 `configs/experiment.yaml`。数据生成器支持 schema v2、质量异常、流量突发和部署漂移注入；输出配置、随机种子、Docker 镜像 tag、Kafka offsets、Spark event log 和 `raw_runs.csv` 必须归档。每场景至少运行五次，报告均值与 95% CI。

## 8. 安全边界

所有服务密码经 `.env` 注入，仓库只提交 `.env.example`。生产部署应启用 Kafka TLS/SASL、MongoDB 身份验证、MinIO access policy、网络隔离和 K8s Secret；不得将密码嵌入 Python 代码或镜像。
